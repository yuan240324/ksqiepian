# 08 - 快手三风格切片（成片包装）

> 阶段：拿到原始精彩片段（clip/raw）之后，包装成可直接发布到快手的竖屏成片。
> 本轮产出 3 条不同风格成品，素材横跨两场直播回放（9/13 笑场 + 9/16 王者荣耀直播）。

---

## 1. 需求与产出

需求：快手平台，三种风格各做一条，素材可从多场直播回放里挑。

### 1.1 第一版（基础包装）

成品（1080x1920 @ 30fps，H.264 + AAC，均带片头钩子与结尾引导）：

| 成品 | 风格 | 素材 | 片长 | 大小 |
| --- | --- | --- | --- | --- |
| 快手_综艺花字风_笑到失控.mp4 | 综艺花字 | 9/13 直播笑场 130~165s | 35.4s | 24.9 MB |
| 快手_解说盘点风_团战名场面.mp4 | 解说盘点 | 9/16 团战 713~748s | 35.4s | 21.0 MB |
| 快手_土味老铁风_社死瞬间.mp4 | 土味老铁 | 9/16 社死弧线 129~159s | 30.4s | 13.4 MB |

### 1.2 第二版（增强版：音效 + BGM + 节拍驱动）

第一版交付后反馈"差点意思"，逐个排查出三个层面的问题并做增强：

```text
① 听觉层：只有原声，没有 BGM 和音效 → 加合成 BGM + 音效库 + 原声闪避
② 节奏层：事件时间点随意 → 全部对齐节拍点，音画同步
③ 视觉层：画面近四成是模糊延展带 → 改为裁满屏（详见 3.1）
```

增强版成品（均已替换第一版，输出目录同名升级）：

| 成品 | 风格 | 素材 | 片长 | 大小 |
| --- | --- | --- | --- | --- |
| 01_综艺花字版_满屏.mp4 | 综艺花字 | 9/16 团战段 | 34.0s | 16.0 MB |
| 02_解说盘点版_满屏.mp4 | 解说盘点 | 9/16 团战段 | 34.0s | 23.7 MB |
| 03_土味老铁版_满屏.mp4 | 土味老铁 | 9/13 笑场段 | 31.0s | 25.3 MB |

> 增强版与第一版的关键差异：**音画全部由节拍驱动** + **画面裁满屏无模糊带**。

三种风格的定位差异：

| 风格 | 视觉语言 | 目标观感 |
| --- | --- | --- |
| 综艺花字 | 彩色描边花字弹窗 + 飘屏弹幕 + 黄色大标题 | 热闹、有梗、"名场面"感 |
| 解说盘点 | 底部字幕条（关键词高亮）+ 直播时间戳 + 盘点角标 | 冷静叙事、"注意看"体 |
| 土味老铁 | 红金大字报横幅 + 跑马灯 + 红框 + 五星 | 接地气、"老铁666"味 |

---

## 2. 素材怎么挑出来的

延续 `docs/07-自动切片.md` 的方法：**音频 RMS 能量聚类定候选，密抽帧人工核对定终选**。

### 2.1 9/16 王者荣耀直播（86 分钟）

对 `analysis.json` 的 571 个高能量秒做聚类排序（`score = n * (0.6 + 0.4*density)`），Top 窗口：

```text
105-137s   score=22.6  开场大厅互动（主播全程笑着聊天）
4223-4255s score=21.4  收尾抽奖/皮肤商店
283-310s   score=17.7  匹配/选人
588-615s   score=17.7  皮肤展示
658-677s   score=14.7  对局前期
719-746s   score=14.3  团战 → 赢 → 推基地
```

密抽帧（每 4s 一帧）核对后确认两条可用叙事线：

- **713-748s 团战线**：团战爆发 → 打赢 → 追击 → 上高地推基地，画面特效密集，
  适合解说风（"注意看"体正好配连续操作）。
- **129-159s 社死线**：主播表情弧线为 **大笑 → 震惊 → 捂脸 → 苦笑恢复**，
  完整的"绷不住了"情绪过山车，适合土味风大字报逐段盖章。

### 2.2 9/13 直播笑场

沿用上一轮结论：130~165s 为憋笑 → 大笑抓头 → 收敛的完整弧线，竖屏原生素材，
继续作为综艺花字风的底。

> 教训：**音频能量只能定位"哪里有事"，必须抽帧确认"是什么事"**。
> 9/16 的最高分窗口 105-137s 其实是坐着聊天（视觉平淡），而 719s 团战分数不高
> 但视觉最炸。选段要音频分和画面信息两条腿走路。

---

## 3. 渲染引擎：styler/style_clip.py

单脚本三风格，风格由 JSON 配置驱动（`cfg_<style>.json`）：

```powershell
# 通用入口
python styler/style_clip.py <raw.mp4> <out.mp4> <cfg.json>

# 实际三条
python styler/style_clip.py raw_0913_0130.mp4 快手_综艺花字风_笑到失控.mp4  cfg_variety.json
python styler/style_clip.py raw_0916_0713.mp4 快手_解说盘点风_团战名场面.mp4 cfg_commentary.json
python styler/style_clip.py raw_0916_0129.mp4 快手_土味老铁风_社死瞬间.mp4   cfg_rustic.json
```

### 3.1 竖屏 9:16 构图（横竖通吃）

两场素材比例不同：9/13 是 1440x1800 竖屏 4:5，9/16 是 1280x960 横屏 4:3。
统一策略：

```text
1) 原画面按宽 1080 缩放，垂直居中（content 高度 ch 自适应）
2) 上下空隙用"放大 + 高斯模糊"的同源画面填充（仿剪映背景虚化）
3) 花字/字幕条落在上/下延展带，不遮挡原始内容
```

好处：横屏游戏直播也能出竖屏成品，且字幕条有干净的落点。

### 3.2 三风格的公共动效

- **弹入**：ease-out-back（过冲回弹），0.3~0.4s 内完成，模拟综艺字幕"砸"上来
- **打字机**：解说字幕逐字出现
- **跑马灯**：土味风格底部红条循环滚动
- 全部用 PIL 自绘（`msyhbd.ttc` / `simhei.ttf`），不依赖外部 ffmpeg 滤镜

### 3.3 字体选择

```text
综艺/解说  微软雅黑 Bold (msyhbd.ttc)   字重够、笔画现代
土味       黑体 SimHei (simhei.ttf)     棱角分明、大字报感
注意       Windows 自带的 STHUPO/STXINWEI 等艺术字体本机没有，别写死
```

---

## 4. 本轮踩的三个坑

### 4.1 C 盘写满导致所有写盘操作失败（Errno 28 / os error 112）

工作目录原本在 `C:\Users\weido\.trae-cn\work\`，C 盘 222.5GB 用满、0 字节可用，
连写脚本本身都失败。清理 TEMP 和缩略图只够喘息。

**解法：工作区整体迁到 G 盘**（`G:\trea\切片\_工作区\`），脚本、中间产物、
渲染输出全部走 G 盘（208GB 可用）。大文件视频处理不要把工作目录放系统盘。

### 4.2 9/16 素材 mux 报 EINVAL（Invalid argument, ret=22）

**现象**：解说风渲染到一半 `cout.mux(pkt)` 抛 `av.error.ArgumentError`；
同一份代码、同一输出目录，土味风（同为 9/16 源）却正常。综艺风也正常。

**排查**：

```text
1) 先怀疑 pts 非单调 → 解码扫描三份 raw，全部单调，排除
2) 无叠加最小复现（muxtest.py）→ 仍复现，锁定与文字渲染无关
3) 对比帧间隔分布 → 找到真凶：
   raw_0916_0713.mp4: 17ms x17 / 33ms x701 / 34ms x342 / 50ms x18 混杂
                       → 量化到 1/30s 网格时 359 对帧碰撞 → 零时长包 → mux 拒收
   raw_0916_0129.mp4: 33/34ms 干净分布，0 碰撞 → 所以正常
```

**修复**（`style_clip.py` 主循环）：输出 pts 量化到 `Fraction(1, 30)` 网格，
碰撞帧直接丢弃：

```python
tick = int(round(t * FPS))
if tick <= last_tick:   # 与上一帧落进同一个 1/30 格，丢掉
    n += 1
    continue
last_tick = tick
nf.time_base = Fraction(1, FPS)   # 不再用 1/1000000
nf.pts = tick
```

时长仍按真实时间轴推进（帧数略减、时间点不变），音画保持同步。
直播回放这种"网络分片拼接"出来的源，时间戳抖动是常态，重编码前先查帧距直方图。

### 4.3 弹幕配置字段顺序

`cfg_variety.json` 里弹幕写成 `[文本, 起始, 结束, y比例, 速度]`，代码却按
`for s, e, txt, ...` 解包 → `TypeError: '<=' not supported between 'str' and 'float'`。
统一为文本在前。JSON 数组没有键名，配置格式和解析顺序要写进文档约定。

### 4.4 土味风横幅溢出 + 跑马灯被内容区遮挡

首轮渲染后抽帧发现两处视觉缺陷：

```text
1) 顶部大字报"老铁们！注意看！"最后一个感叹号溢出红色边框
   原因：横幅层宽写死 w+90，未对文字宽度做约束
   修复：banner() 增加 max_w 参数，超宽时按比例回退字号并重算层尺寸；
         星标字号改为跟随横幅高度（max(30, h*0.52)），不再写死 54
2) 底部跑马灯完全看不到
   原因：marquee 的 y 写死 1752，落进了 9:16 构图里"上下模糊延展带"的下带，
         而该带被内容区贴图覆盖（横屏源 content 高 810、cy=555，
         画面内容实际延伸到 y=1365 以下才结束 → 1752 被压在画面外）
   修复：draw_marquee() 接收 geo，y 动态取 geo.cy + geo.ch - 68，
         即紧贴内容区底边，成为压在画面上的一条红条（也更像短视频跑马灯）
```

经验：**竖屏构图里的"坐标"必须相对内容区 geo 计算，不能写死绝对像素**。
横竖屏源的 content 高度不同（1350 vs 810），写死坐标必然在某一类源上穿帮。

### 4.5 增强版：模糊延展带占近四成画面（"没填满"的根因）

**现象**：第一版抽帧复核时发现，画面上下各有约 18% 的模糊延展带，两条字幕分居
两端被模糊带割裂，花字压顶，跑马灯文字被截断。合计近四成画面是"空"的——
这正是"成片差点意思"的主因。

**根因**：第一版统一"按宽 1080 缩放 + 上下模糊补带"，对 1280x960 横屏源来说，
按宽缩放后内容只有 798px 高，剩下 1122px（58%）全是模糊带。

**修复**：重写构图函数 `layout()`，改为**裁切优先**：

```python
def layout(sw, sh, crop_top=0, crop_bot=0, fill=0.985, band=0.0):
    vh = sh - crop_top - crop_bot
    src_ar = sw / float(vh)
    MAXUP = 2.0          # Lanczos 放大上限，超过就越裁越糊
    if src_ar <= W / float(H):
        # 竖屏源：按宽铺满，上下裁一点
        s = (W * fill) / float(sw)
        cw, ch = int(round(sw * s)), int(round(vh * s))
        if ch > H:       # 太高：按高铺满并左右裁，但不超放大量
            s = min(MAXUP, (H * fill) / float(vh))
            ...
    # 横屏/方屏源：按高铺满、左右裁掉多余部分
    s = (H * fill) / float(vh)
    vw = W / s
    if s > MAXUP or vw > sw:   # 放不下：退回按宽铺
        ...
```

核心思想：**按源比例自动挑方向**，横屏源按高铺满、竖屏源按宽铺满，
让 `ch ≈ H`、`cw ≈ W`，模糊带只剩 fill 余量的 1.5%。

**验证结果**（`check_layout.py`，含越界 assert 保护）：

```text
0913竖屏1440x1800  content=1064x1891  scale=1.05  模糊带 1.5%  横裁 206px
0916横屏1280x960   content=1064x1891  scale=1.97  模糊带 1.5%  横裁 366px
0713横屏1280x960   content=1064x1891  scale=1.97  模糊带 1.5%  横裁 366px
```

模糊带从 **30~58% 降到 1.5%**。横屏源放大 1.97 倍是必要代价（源本身只有 1280x960），
2x 以内的 Lanczos 上采样观感可接受。

**连带修复的三处视觉缺陷**：

```text
1) 文字元素全部改为锚定内容区（geo.cy / geo.ch），不再按整屏定位
   花字 y = geo.cy + ch*0.32   字幕 y = geo.cy + ch - 48   角标 y = geo.cy + 20
2) 跑马灯接缝切字
   原因：原实现按 tw + 90 循环贴图，接缝处把字切一半
   修复：改用 tile = max(W, tw) 作为周期单位，字在 tile 内居中
3) 字幕条截断 + 与跑马灯重叠
   修复：自动缩字号（while size > 26 ... size -= 2），绝不截断；
         跑马灯开启时字幕自动上移到 geo.ch - 148
```

> 教训：**构图策略要在交付前用三条素材的数学验证跑一遍**，
> 不能只看"能不能渲染出来"。第一版三条都能出片，但模糊带占比天差地别
> （竖屏 30% vs 横屏 58%），只看一条会漏判。

---

## 4.6 增强版：音效 + BGM + 节拍驱动

### 4.6.1 音效库（`sfx.py`，纯 NumPy 合成）

不依赖外部素材，用波形合成 8 种音效：

| 音效 | 合成方式 | 用途 |
| --- | --- | --- |
| whoosh | 白噪声 + 带通扫频 | 转场、推近 |
| boom | 低频正弦 + 快速衰减包络 | 爆点、震屏 |
| ding | 双正弦叠加 + 指数衰减 | 花字出现 |
| wow | 频率上滑正弦 | 反转、惊讶 |
| clap | 噪声脉冲串 | 掌声感 |
| drumroll | 密集噪声脉冲 | 悬念铺垫 |
| rise | 频率渐升 + 音量渐强 | 冲向爆点 |
| sub | 极低频正弦 | 下坠、重击 |

### 4.6.2 自造 BGM（三风格各一条）

```text
综艺：大调和弦进行 + 明亮琶音 + 四四拍底鼓
解说：小调进行 + 沉稳低音脉冲 + 稀疏打击
土味：五声音阶 + 方波主音 + 快节奏打点
```

结构：和弦垫底 → 琶音铺面 → 低音脉冲打点，三段叠加后做 tanh 软限幅。

### 4.6.3 混音策略（`audiomix.py`）

```text
原声：全段保留，在音效/爆点处做"闪避"（ducking）包络，让位给音效
BGM ：整段铺底，爆点处小幅提升（+2dB）
音效：按 beats 时间点叠加，峰值对齐
总线：三段相加 → tanh 软限幅 → 峰值归一化到 0.84
```

实测三条的混音占比：

```text
综艺  orig 0.130  bgm 0.059  sfx 0.061  -> peak 0.840
解说  orig 0.173  bgm 0.073  sfx 0.060  -> peak 0.840
土味  orig 0.122  bgm 0.061  sfx 0.064  -> peak 0.840
```

### 4.6.4 节拍驱动（`plan_*.json`）

所有视觉/听觉事件都写进 plan JSON 的 `beats` 数组，渲染时按时间点触发：

```json
"beats": [
  {"t": 0.0,  "kind": "whoosh"},   // 片头转场音
  {"t": 3.2,  "kind": "boom"},     // 爆点：震屏 + 闪白 + 咚
  {"t": 5.0,  "kind": "pop"},      // 花字出现：轻震 + 叮
  {"t": 32.5, "kind": "end"}       // 结尾卡：音乐收束
]
```

好处：**改节奏只改 JSON，不动代码**；音效、震屏、闪白、花字出现时刻天然对齐。

### 4.6.5 PyAV 音频编码坑

fltp 平面格式要求 shape 为 `(channels, samples)`，而 NumPy 默认给的是
`(samples, channels)`。直接传会报错或输出静音：

```python
blk = np.ascontiguousarray(blk.T)   # (samples, 2) -> (2, samples)
```

---

## 4.7 增强版：视觉特效（`vfx.py`）

| 特效 | 实现 | 作用范围 |
| --- | --- | --- |
| 震屏 | 内容层随机偏移 | 只抖内容层，花字保持稳定 |
| 推近 | 内容层按区间缓慢放大 | 内容和花字分离 |
| 闪白 | 全屏白色叠加 + 快速衰减 | 全屏 |
| 暗角 | 径向渐变遮罩 | 焦点跟随内容层中心 |
| 饱和度/对比度 | 逐通道线性映射 | 内容层 |

> 关键设计：**震屏只作用于内容层（`geo` 层），花字贴在最上层**。
> 这样"画抖字稳"，符合快手观感；如果整屏一起抖，字幕会糊成一团。

---

## 5. 成品校验方式

```powershell
# 从成品里抽关键时间点帧，拼联系表人工核对
python styler/sheet.py <帧目录> <out.jpg> 6
```

三条成品全部抽帧核对通过：

| 成品 | 抽帧数 | 核对结论 |
| --- | --- | --- |
| 综艺花字风 | 7 帧 | 竖屏构图、黄色大标题、彩色花字（绷不住了/哈哈哈哈哈/笑到抓头）、飘屏弹幕、结尾"关注主播 不迷路"卡片全部正常 |
| 解说盘点风 | 8 帧 + 底部放大复核 | 左上角标"王者名场面·第1期"、右上"9月16日 直播回放"、右下直播时间戳（12:04→12:30 递增）、钩子"高能团战来了"、结尾"关注看下期名场面"正常；字幕条「对面血条瞬间**见底**」关键词黄色高亮生效（联系表缩略图过小易误判为缺失，已按像素裁带放大确认） |
| 土味老铁风 | 6 帧（修复后复检） | 红金双层边框完整、大字报"★老铁们！注意看！★"完整落在框内、五个花字弹窗（笑喷了/我去？！/捂脸了家人们/尴尬住了/哈哈哈666）、底部跑马灯滚动条、结尾卡"点关注 不迷路 / 双击666支持一下"均正常 |

> 校验提示：联系表缩略图（单格 250px 宽）会看不清 42px 左右的字幕条，
> **判定"元素缺失"前先按像素裁出目标区域放大看**，避免误判。

### 5.1 构图数学预检（增强版新增）

渲染前先用 `check_layout.py` 跑一遍三条素材的构图数学，含越界 assert：

```python
assert cy + ch <= R.H, "内容底越界!"
assert crop_x >= 0 and crop_x + int(round(cw / scale)) <= sw, "横向裁框越界!"
```

这一步能在**不渲染**的情况下暴露构图错误——第一版就是漏了这步，
三条都能出片但模糊带占比从 30% 到 58% 不等。

### 5.2 增强版抽帧复核

三条增强版各抽 6 帧（覆盖片头/花字/字幕/跑马灯/结尾五个阶段）：

| 成品 | 抽帧点 | 核对结论 |
| --- | --- | --- |
| 01 综艺花字版 | 0.6 / 4.2 / 9.1 / 16.5 / 24.3 / 31.8s | 画面铺满无模糊带、"笑到失控"/"笑到抓头"花字居中清晰、角标"综艺名场面·第1期"与"9月13日 直播回放"正常、直播时间戳递增 |
| 02 解说盘点版 | 0.6 / 5.3 / 12.8 / 20.4 / 28.9s | 画面铺满、花字"注意看这个走位"清晰、字幕托盘"对面"/"这就是职业级操作"完整不截断、时间戳 11:52:22→11:52:38 递增 |
| 03 土味老铁版 | 0.5 / 3.6 / 8.4 / 15.2 / 22.6 / 29.4s | 上下双人分屏铺满、花字"尴尬住了"居中、跑马灯"进来右上角幸运星参与抽奖 战令 史诗 传说"完整滚动不切字、红底跑马灯条压在内容区底边 |

---

## 6. 文件清单

```text
styler/                         第一版：单脚本三风格（配置驱动）
├── style_clip.py        ★ 三风格渲染引擎（PIL 自绘 + libx264/aac 重编码）
├── cfg_variety.json     综艺花字风配置
├── cfg_commentary.json  解说盘点风配置
├── cfg_rustic.json      土味老铁风配置
└── sheet.py             帧目录 → 联系表（人工核对用）

pro_edit/                       第二版：增强版工具链（节拍驱动）
├── render_pro.py        ★ 增强版渲染器（节拍驱动 + 裁满屏构图）
├── sfx.py               音效合成库（whoosh/boom/ding/wow/clap/drumroll/rise/sub）
├── audiomix.py          混音器（原声闪避 + BGM + 音效 → tanh 限幅）
├── vfx.py               视觉特效（震屏/推近/闪白/暗角/调色）
├── tex.py               文字特效（花字/逐字弹跳/打字机/渐变描边）
├── grab_frames.py       抽帧核对工具
├── plan_variety.json    综艺风编排（节拍 + 花字 + 字幕 + 钩子）
├── plan_commentary.json 解说风编排
└── plan_rustic.json     土味风编排（含跑马灯 + 大字报）

工作区（G 盘，不入库）：
├── raw_0913_0130.mp4    9/13 笑场原始段 1440x1800@60
├── raw_0916_0713.mp4    9/16 团战原始段 1280x960@30
└── raw_0916_0129.mp4    9/16 社死原始段 1280x960@30

成品：G:\trea\切片\快手三风格\
├── 01_综艺花字版_满屏.mp4    34.0s  16.0 MB   ← 增强版
├── 02_解说盘点版_满屏.mp4    34.0s  23.7 MB   ← 增强版
└── 03_土味老铁版_满屏.mp4    31.0s  25.3 MB   ← 增强版
```

增强版渲染入口：

```powershell
python pro_edit/render_pro.py <raw.mp4> <out.mp4> <plan.json>
```

### 6.1 plan JSON 字段约定（增强版）

| 字段 | 说明 |
| --- | --- |
| style | variety / commentary / rustic |
| dur | 成片时长（秒） |
| src_off | 源视频起始偏移（秒） |
| beats | 节拍点数组：`{t, kind}`，kind ∈ boom/pop/whoosh/end |
| pops | 花字：`{txt, t, dur, cx, cy, size, fill, sw}`，cy 为内容区比例 |
| titles | 大标题：`{txt, t, dur, cy, size}` |
| subs | 字幕条：`{txt, t, dur, hl}`，hl 为高亮关键词 |
| hook | 片头钩子：`{txt, t, dur}` |
| end | 结尾卡：`{t, l1, l2}` |
| marquee | 跑马灯：`{txt, t0, speed}` |
| banner | 大字报：`{txt, t0, t1}` |
| chip / chip2 | 左上/右上角标：`{t}` |
| grade | 调色：`{sat, con, vig}` |
| push | 推近区间：`[[t0, t1, amount]]` |
| shakes | 额外震屏：`[[t, 时长, 幅度]]` |
| fill | 构图填充系数（默认 0.985，即保留 1.5% 余量） |
